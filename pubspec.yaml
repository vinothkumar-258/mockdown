name: kashcool_markdown
description: "Markdown + LaTeX math + chemistry renderer for Flutter"
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_markdown_plus: ^1.0.5
  markdown: ^7.3.0
  flutter_math_fork: ^0.7.4
  gpt_markdown: ^1.1.4

flutter:
  uses-material-design: true

dev_dependencies:
  flutter_test:
    sdk: flutter
